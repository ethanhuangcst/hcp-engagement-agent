# 制药行业 HCP Personalized Engagement Assistant

- 为药企量身定做的，为 HCP (Health Care Professional) 建立数字分身，提供个性化 Engagement 的建议
- 内建 hcp-twin-mcp MCP，全网检索 HCP 的信息（个人信息、职业轨迹、科研动向、活动热力、级别标签等），生成数字分身与 HCP 洞察
- 搭建 medical-knowledge-base RAG：**按专科按需**灌注前沿学术资料 + HCP Engagement 合规文件（不做全科预灌）
- 内建 hcp-engagement-agent 智能体，根据数字分身与洞察推荐个性化 engagement 建议

# 产品包含：

## Web App「HCP Personalized Engagement Assistant」交互入口

顶栏默认两项；可**多开**分身姓名标签。洞察与一人一策在分身工作台内，不作为全局顶栏 Tab：

| 顶栏 / 工作台 | 职责 |
|---------------|------|
| **HCP数字分身** | 已建分身列表（列表本身不算打开工作台）；行内仅「打开」；新增进入空白录入→查询消歧→确认保存 |
| **{姓名}**（可多开） | 打开后插入的姓名标签；悬停 `×` 关闭本页；工作台子 Tab：**HCP资料** / **HCP洞察** / **一人一策**；右侧修改·删除·关闭本页；「智能体情报构建」常驻区仅在 HCP资料 |
| **HCP Engagement Agent** | **通用**开放对话（不绑定当前分身）；历史/正文 localStorage；附件上传、面板可 resize |

界面默认 **zh-CN**；职业轨迹等展示文案随 locale 本地化，预留 i18n 扩展（见 `6.ui-guideline` / `app-design`）。

## hcp-twin-mcp MCP

- 全网采集公开信息：职业轨迹、文献、科研动态、活动热力等
- 写入香港 Postgres 数字分身；绑定文献检索号（AuthorIds）
- **级别打标**（`hcp_tier` + `role_tags`）：按公开影响力分层，禁止处方潜力打标
- 形成洞察：在做什么、对什么感兴趣、可能想要什么

### HCP 文献查询站点（按优先级）

**准入**：公开 API · 国内可访问 · 免登录（不做爬虫）。

| 优先级 | 站点 | 网址 | 用途 |
|--------|------|------|------|
| P0 | PubMed | https://pubmed.ncbi.nlm.nih.gov/ | 生物医学主库；PMID / 作者簇 |
| P0 | ORCID | https://orcid.org/ | 作者唯一号；拉作品 |
| P0 | OpenAlex | https://openalex.org/ | 作者 / 作品图谱 API |
| P1 | Europe PMC | https://europepmc.org/ | PubMed 互补 |
| P1 | Crossref | https://www.crossref.org/ | DOI 元数据归并 |
| P2 | ClinicalTrials.gov | https://clinicaltrials.gov/ | 试验旁证 |

消歧顺序：`orcid` → PubMed 作者簇 → `openalex` → Europe PMC / Crossref。详见 [`literature-sources.md`](../knowledge/mcp-hcp-virtual-twin/literature-sources.md)；设计 [`mcp/mcp-design.md`](./mcp/mcp-design.md)。

## medical-knowledge-base RAG

- **按专科按需**学术索引（Twin specialty/themes 触发）；同专科缓存复用
- HCP Engagement 合规文件（手灌种子 + 租户 SOP）

## hcp-engagement-agent 智能体

- 根据数字分身洞察 + 学术 RAG + 合规 RAG
- 推荐个性化一人一策；支持对话迭代与附件上下文

## 典型用例

复旦大学附属中山医院 朱同玉教授

---

## 功能 Index（规格入口）

| 子系统 | 功能规格 | 设计 | 用户故事 / 提示 |
|--------|----------|------|-----------------|
| **Web / BFF** | [`app/app-function-spec.md`](./app/app-function-spec.md)（F-WEB-*） | [`app/app-design.md`](./app/app-design.md) · 原型 [`app/hcp-ui-prototype.canvas.tsx`](./app/hcp-ui-prototype.canvas.tsx) | [`app/app-stories.md`](./app/app-stories.md)（US-WEB-*） |
| **hcp-twin-mcp** | [`mcp/mcp-function-spec.md`](./mcp/mcp-function-spec.md)（F-MCP-*） | [`mcp/mcp-design.md`](./mcp/mcp-design.md) | — |
| **medical-kb（RAG）** | [`rag/rag-function-spec.md`](./rag/rag-function-spec.md)（F-RAG-*） | [`rag/rag-design.md`](./rag/rag-design.md) | 研究：[`../knowledge/rag-medical-knowledge-base/ingest-feasibility-and-ops.md`](../knowledge/rag-medical-knowledge-base/ingest-feasibility-and-ops.md) |
| **hcp-engagement-agent** | [`agent/agent-function-spec.md`](./agent/agent-function-spec.md)（F-AGT-* / I-AGT-*） | [`agent/agent-design.md`](./agent/agent-design.md) | [`agent/initial-prompt.md`](./agent/initial-prompt.md) |

横切：[`3.architecture.md`](./3.architecture.md) · [`4.install-dependencies.md`](./4.install-dependencies.md) · [`5.hcp-twin-data-dictionary.md`](./5.hcp-twin-data-dictionary.md) · [`6.ui-guideline.md`](./6.ui-guideline.md) · [`7.test-strategy.md`](./7.test-strategy.md)（测试策略与 SMART DoD）

---

## MVP 划分（4 个可独立闭环）

### 闭环原则（强制）

1. **一批一事**：每个 MVP 只交付一条可演示的用户/运维闭环；验收一句话必须在本批内可跑通。
2. **禁止前向依赖**：MVP-N **不得**依赖 MVP-N+1 的能力才能「看起来完成」（禁止 stub 冒充下级 MVP）。
3. **允许后向依赖**：MVP-N 可以依赖 **已验收** 的 MVP-1…N-1；上游未就绪时用 **fixture / mock**，且 fixture 路径写进本批验收标准。
4. **缺能力就移出本批**：若某 UI/文案暗示下级能力，该能力必须与触发它的管线同批交付，或从本批删除。

### 批次对照（历史 → 现行 4 批）

| 更早 | 上一版（5 批） | **现行（4 批）** | 说明 |
|------|----------------|------------------|------|
| 旧 7 批之 1–2 | MVP-1 身份 + MVP-2 情报构建 | **MVP-1** Twin 工作台 | 身份 CRUD 与 `build_twin`/真进度 **同批**；产品路径 live |
| 旧 7 批之 3 | MVP-3 | **MVP-2** HCP 洞察工作台 | 洞察八块；可用 fixture Insights |
| 旧 7 批之 4–5 | MVP-4 | **MVP-3** 知识库 | 合规底座 + 学术按需 |
| 旧 7 批之 6–7 | MVP-5 | **MVP-4** Engagement | 一人一策 + open_chat |

建议实现顺序：**MVP-1 → 2**；**MVP-3 可与 2 并行**；**MVP-4 在 2+3 之后**（Insights fixture + 合规 retrieve 即可验收）。

| MVP | 名称 | 独立闭环（验收一句话） | 主覆盖功能 | 依赖 |
|-----|------|------------------------|------------|------|
| **MVP-1** | Twin 工作台（身份 + 情报） | 多开姓名标签 + 工作台子 Tab；列表仅打开；修改/删除在工作台；详情触发 `build_twin`；职业轨迹按 locale 展示；浏览器只调 BFF；产品 `TWIN_MODE=live` | F-WEB-001–016、035–036、040、045–046；F-MCP-001–035 | `domain` + `db` + live MCP |
| **MVP-2** | HCP 洞察工作台 | 洞察 Tab 八块可读可链；`synthesizeDoingNow` 与分身同源；洞察/一人一策页壳可返回分身；**验收允许 fixture Insights**（不要求本批现场重跑 Stage） | F-WEB-017–025、037、041、044；F-AGT-001、001a–d、005、008–010、028 | 已验收的 MVP-1；或 Insights fixture |
| **MVP-3** | 知识库（合规 + 学术按需） | 手灌合规种子 → `retrieve_compliance` 命中含 `clause_id`；专科 on-demand → `retrieve_academic` 可过滤命中；可选 F-WEB-039 触发可观测 | F-RAG-001–027（含 002a–d）；F-WEB-039 | Qdrant + corpus；可与 MVP-2 并行 |
| **MVP-4** | Engagement（一人一策 + Agent） | 生成 3–5 方案（双引用）→ 合规闸门 → `revise_options`（绑方案）；**通用** `open_chat` 多轮 + 本机历史/附件/resize/typing | F-WEB-026–034、038、042–043；F-AGT-002–007、011–027、019、022–023、029–030；F-RAG-021–022、025 | **已验收的 MVP-2 + MVP-3**（学术可 sparse） |

### 本批明确不做（防假闭环）

| MVP | 本批禁止出现 |
|-----|----------------|
| MVP-1 | 确认保存后用 stub 假装构建已完成；产品默认 mock fixture 冒充 live 消歧/构建 |
| MVP-2 | 要求 live 爬完才出洞察；合规检索 / Options |
| MVP-3 | 一人一策生成；开放对话 |
| MVP-4 | 正式 MLR 电子签、CRM 回写 |

### MVP 与用户故事

| MVP | US-WEB | US-MCP | US-RAG | US-AGT |
|-----|--------|--------|--------|--------|
| MVP-1 | 001–004（含构建进度与 enrichment） | 001–006 | — | — |
| MVP-2 | 005 | — | — | 001–002 |
| MVP-3 | 039（可选） | — | 001–004 | — |
| MVP-4 | 006、007、008 | — | 005 | 003–006 |

故事文件：[`app/app-stories.md`](./app/app-stories.md) · [`mcp/mcp-stories.md`](./mcp/mcp-stories.md) · [`rag/rag-stories.md`](./rag/rag-stories.md) · [`agent/agent-stories.md`](./agent/agent-stories.md)

### 刻意不进前 4 个 MVP 闭环的能力

- 正式 MLR 电子签、CRM 回写、自动办会
- 全科 / 全 PubMed 预灌、知网无授权爬取
- Parent-Document（F-RAG-022）可作为 MVP-3 之后增强

各功能规格「实现批次」列填 **MVP-1…MVP-4**，与本表一致。

---

## 功能完成情况（as_of 2026-07-18 · 按现行 4 批）

状态约定：`未开始` · `进行中` · `包侧完成`（库/脚本可演示，缺 Web 闭环） · `闭环完成`（含 DoD：测试、联调、文档；用户验收另记）。

| MVP | 名称 | 状态 | 说明 |
|-----|------|------|------|
| **MVP-1** | Twin 工作台（身份 + 情报） | 包侧+Web 近闭环（待正式 UAT/commit） | 身份 CRUD + live resolve/confirm；`build_twin` / 真进度 UI / Stage A–E 落库已接线；产品禁止默认 mock（CI 须 `ALLOW_TWIN_MOCK`） |
| **MVP-2** | HCP 洞察工作台 | 包侧+Web 闭环（待正式 UAT/commit） | 洞察八块 UI + `GET /api/insights` + `POST /api/insights/doing-now`；无 Key 时规则合成；与详情同源 `doing_now`；打印导出；洞察/一人一策页首尾返回分身（F-WEB-044） |
| **MVP-3** | 知识库 | 近闭环（待正式 UAT/commit） | 合规种子 + `retrieve_compliance`；学术 `ingest_on_demand` / `retrieve_academic`；F-WEB-039 确认后触发与状态轮询 |
| **MVP-4** | Engagement | 近闭环（待正式 UAT/commit） | Options 生成/选项卡、合规闸门、`revise_options`（本机按方案存聊天）、**通用** `open_chat`（不绑分身；本机 localStorage 索引+正文）、typing/Enter 发送、附件元数据、面板 resize；Postgres `engagement_options` / `chat_sessions`（open_chat 落 `_agent_general`） |

### 包级进度（支撑上述 MVP）

| 包 / 资产 | 状态 | 对应 |
|-----------|------|------|
| `@hca/domain` | 包侧完成 | MVP-1；含 `DoingNow` |
| `@hca/db` | 包侧完成 | MVP-1、MVP-3、MVP-4 |
| `@hca/hcp-twin-mcp`（含 confirm / build Stage A–E） | 包侧完成（live） | MVP-1；CI 单测可注入 HTTP |
| `@hca/mcp-client` | 包侧完成 | MVP-1 |
| `@hca/medical-kb` | 包侧完成（合规 + 学术按需） | MVP-3 |
| `@hca/hcp-engagement-agent`（LlmClient + Options/Chat） | 包侧完成（无 Key 可规则降级） | MVP-2、MVP-4 |
| `apps/web` + BFF | 近闭环（身份 + 构建 + 洞察 + Engagement） | MVP-1–4 |

### MVP-3 已覆盖功能（对照 · 合规 + 学术切片）

F-RAG-001–011、002a–d、013–020、023–024、026–027 · I-RAG-001–005 / 007 · US-RAG-001–004 · F-WEB-039（BFF ingest + 确认后触发 + 状态轮询）。  
验收默认已降级：进程内小 embedding/rerank、进程内异步互斥、学术 fixture/摘要优先（见 [`ToDo.md`](./ToDo.md)）。  
**未含 / 后置**：租户 SOP / Parent / Eval；NCBI 完整限速、更大模型、BullMQ（见 ToDo）。

### MVP-4 已覆盖功能（对照）

F-WEB-026–034、038 · F-AGT-002–007、011–027、019、022–023、029 · I-AGT-002–004、006–010 · US-WEB-006–007。  
无 Key 时规则提案/修订/开放对话；提案路径强制双路 retrieve（软失败则附条件闸门）。  
`open_chat` 为通用 Agent（不注入 Twin）；分身级修订仅 `revise_options`。浏览器侧：一人一策与 Agent 聊天均 localStorage；服务端仍写 `chat_sessions`。  
**后置**：F-RAG-021/022/025；SSE 流式为可选增强（P0 非流式已持久化）。

### MVP-2 已覆盖功能（对照）

F-WEB-017–025、037、041、045 · F-AGT-001、001a–d、005、008–010、028 · I-AGT-001 / I-AGT-005 · US-WEB-005 · US-AGT-001–002。
